name: CD (TestFlight)

# main への push で TestFlight へ配信する。手動実行も残しておく。
# ※ workflow_dispatch はデフォルトブランチにこのファイルが無いと選択肢に出ない
#   仕様のため、main へマージするまで手動実行ボタンは現れない。
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'LICENSE'
      - '.github/workflows/ci-main.yml'

# 配信が二重に走るとビルド番号が衝突するため、同時実行させない。
# アップロード途中でキャンセルすると TestFlight に中途半端な状態が残るので、
# 走り始めた配信は最後まで走らせる。
concurrency:
  group: cd-testflight
  cancel-in-progress: false

jobs:
  testflight:
    name: iOS → TestFlight
    # 🔴 macos-15 は使えない。既定の Xcode 16.4（iOS 18.5 SDK）でビルドすると
    #    App Store Connect が「iOS 26 SDK 以降が必須」として 409 で弾く。
    runs-on: macos-26
    timeout-minutes: 60
    env:
      # fastlane は未設定だと文字化けやクラッシュを起こす。
      LANG: en_US.UTF-8
      LC_ALL: en_US.UTF-8
      APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
      APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
      APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
      MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
      MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
      FASTLANE_SKIP_UPDATE_CHECK: 1
      FASTLANE_HIDE_CHANGELOG: 1
      FASTLANE_OPT_OUT_USAGE: 1
    steps:
      - uses: actions/checkout@v5

      # ruby-version は .ruby-version から読まれる。ローカルと CI で
      # 同じ Ruby を使い、Gemfile.lock の解決結果を一致させるため。
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Show Xcode version
        run: xcodebuild -version

      # 証明書リポジトリは private なので SSH 鍵で取得する。
      #
      # 🔴 このステップに `set -x` を絶対に足さないこと。
      #    GitHub の Secret マスクは「完全一致」にしか効かず、ヒアドキュメントで
      #    展開された鍵の中身はマスクされずログに出る。
      #    デバッグで確認したいときも `cat` せず `ls -la` や `wc -c` で
      #    存在とサイズだけ見ること。
      - name: match 用の deploy key を配置
        env:
          MATCH_DEPLOY_KEY: ${{ secrets.MATCH_DEPLOY_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$MATCH_DEPLOY_KEY" > ~/.ssh/match_deploy_key
          chmod 600 ~/.ssh/match_deploy_key
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
          cat >> ~/.ssh/config <<'EOF'
          Host github.com
            HostName github.com
            User git
            IdentityFile ~/.ssh/match_deploy_key
            IdentitiesOnly yes
            StrictHostKeyChecking yes
          EOF
          chmod 600 ~/.ssh/config

      - name: ビルド番号を決定
        id: build_number
        env:
          BUILD_NUMBER_OUTPUT: ${{ runner.temp }}/build_number
        run: |
          # 標準出力は fastlane 自身のログが混ざるため、値はファイルで受け取る。
          bundle exec fastlane next_build_number
          NUMBER=$(cat "$BUILD_NUMBER_OUTPUT")
          # 想定外の値でビルドが進まないよう、数値であることを確認する
          if ! [[ "$NUMBER" =~ ^[0-9]+$ ]]; then
            echo "::error::ビルド番号の採番に失敗しました: $NUMBER"
            exit 1
          fi
          echo "value=$NUMBER" >> "$GITHUB_OUTPUT"
          echo "採番したビルド番号: $NUMBER"

      - name: ビルドして TestFlight へアップロード
        env:
          BUILD_NUMBER: ${{ steps.build_number.outputs.value }}
        run: bundle exec fastlane beta

      # GitHub-hosted は使い捨て VM だが、ログや後続ステップに鍵が残らないようにする。
      # セルフホストへ移行したときに必須になるため最初から入れておく。
      - name: 後始末
        if: ${{ always() }}
        run: |
          security delete-keychain "$RUNNER_TEMP/fastlane_tmp_keychain-db" || true
          rm -f ~/.ssh/match_deploy_key ~/.ssh/config
